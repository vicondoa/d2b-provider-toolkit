# Canonical contract artifacts

Files under `docs/reference/` are verbatim public contract artifacts from
`vicondoa/d2b` inventory revision
`b1f2c13a196004f5a0fb999808d691b0668cf226`. They are packaged alongside the
byte-identical canonical SDK sources pinned in
`pins/d2b-provider-source.json`.

Do not edit these files locally. Refresh them only as part of an intentional
canonical source pin update and verify the complete distribution fingerprint.
